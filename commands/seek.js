const {SlashCommandBuilder}=require("discord.js"); module.exports={data:new SlashCommandBuilder().setName("seek").setDescription("Jump to a position in the current song.").addIntegerOption(o=>o.setName("seconds").setDescription("Position in seconds").setMinValue(0).setRequired(true)),async execute(i,{music}){const p=music.getPlayer(i.guild.id);if(!p?.queue?.current)return i.reply("❌ Nothing is playing.");await p.seek(i.options.getInteger("seconds")*1000);await i.reply("⏩ Seeked to **"+i.options.getInteger("seconds")+"s**.");}};