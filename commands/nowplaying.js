const { SlashCommandBuilder }=require("discord.js"); const {formatDuration,embed}=require("../music/helpers"); module.exports={data:new SlashCommandBuilder().setName("nowplaying").setDescription("Show the current track."),async execute(i,{music}){const p=music.getPlayer(i.guild.id);const t=p?.queue?.current;if(!t)return i.reply("🎵 Nothing is playing.");await i.reply({embeds:[embed("🎶 Now Playing",`**${t.title}**\nDuration: ${formatDuration(t.length||0)}`)]});}};