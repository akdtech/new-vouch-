const {SlashCommandBuilder}=require("discord.js");module.exports={data:new SlashCommandBuilder().setName("invite").setDescription("Create an invite for this channel."),async execute(i){if(!i.channel?.createInvite)return i.reply("❌ I cannot create an invite here.");const inv=await i.channel.createInvite({maxAge:3600,maxUses:1,reason:"GMAO community invite"});await i.reply(`🔗 ${inv.url}`);}};