const { SlashCommandBuilder }=require("discord.js"); const {embed}=require("../music/helpers"); module.exports={data:new SlashCommandBuilder().setName("queue").setDescription("Show the music queue."),async execute(i,{music}){const p=music.getPlayer(i.guild.id);if(!p)return i.reply("📭 Queue is empty.");const tracks=p.queue.slice(0,15).map((t,n)=>`${n+1}. ${t.title}`).join("\n")||"Nothing queued.";await i.reply({embeds:[embed("🎵 GMAO Music Queue",tracks)]});}};