const {SlashCommandBuilder}=require("discord.js"); module.exports={data:new SlashCommandBuilder().setName("remove").setDescription("Remove a queued track.").addIntegerOption(o=>o.setName("position").setDescription("Queue position").setMinValue(1).setRequired(true)),async execute(i,{music}){const p=music.getPlayer(i.guild.id),n=i.options.getInteger("position")-1;if(!p?.queue?.length)return i.reply("❌ Queue is empty.");if(n>=p.queue.length)return i.reply("❌ That position does not exist.");const t=p.queue[n];p.queue.remove(n);await i.reply("🗑️ Removed **"+t.title+"**.");}};