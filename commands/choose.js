const {SlashCommandBuilder}=require("discord.js");module.exports={data:new SlashCommandBuilder().setName("choose").setDescription("Choose between options.").addStringOption(o=>o.setName("options").setDescription("Separate with |").setRequired(true)),async execute(i){const a=i.options.getString("options").split("|").map(x=>x.trim()).filter(Boolean);await i.reply(a.length?`🎯 **${a[Math.floor(Math.random()*a.length)]}**`:"❌ No options.");}};