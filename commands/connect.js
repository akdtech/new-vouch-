const {SlashCommandBuilder}=require("discord.js"); module.exports={data:new SlashCommandBuilder().setName("connect").setDescription("Reconnect the music bot to your voice channel."),async execute(i,{music}){if(!i.member.voice.channel)return i.reply({content:"❌ Join a voice channel first.",ephemeral:true});await music.join(i.guild,i.member.voice.channel);await i.reply("🔌 DEATH Music 24/7 connected.");}};